[functions]
  node_bundler = "esbuild"
  external_node_modules = ["express", "cors", "dotenv", "swagger-ui-express", "serverless-http"]

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
  force = true
